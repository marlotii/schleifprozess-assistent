name: Create Offline Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version, beispielsweise v2.95.2"
        required: true
        default: "v2.95.2"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Determine version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            VERSION="${GITHUB_REF_NAME}"
          else
            VERSION="${{ inputs.version }}"
          fi
          [[ "$VERSION" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Validate application source
        shell: bash
        run: |
          set -euo pipefail
          test -s index.html
          grep -qi '<!doctype html' index.html
          grep -qi '</html>' index.html
          grep -q '<title>Schleifprozess Assistent</title>' index.html
          test "$(stat -c%s index.html)" -eq 1145485
          test "$(sha256sum index.html | cut -d' ' -f1)" = "22e5cf076ceac8a89b45a3aa174824896c74320269f5f5ffba48876c39dabd4c"
          test "$(git hash-object index.html)" = "04b9ea4689f94bf2e5b3a6be0b2c4f41479ed76b"

      - name: Build offline package
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          PACKAGE="Schleifprozess_Assistent_Offline_${VERSION}"
          rm -rf "$PACKAGE" "$PACKAGE.zip"
          mkdir -p "$PACKAGE/assets"
          cp index.html manifest.webmanifest sw.js README-OFFLINE.txt "$PACKAGE/"
          cp assets/icon-192.png assets/icon-512.png assets/apple-touch-icon.png "$PACKAGE/assets/"
          if [[ -f Schleifprozess_Assistent_2_95_DE_EN_WISSEN_LINKS_PRUEFBERICHT.md ]]; then cp Schleifprozess_Assistent_2_95_DE_EN_WISSEN_LINKS_PRUEFBERICHT.md "$PACKAGE/"; fi
          if [[ -f Schleifprozess_Assistent_2_95_DE_EN_WISSEN_LINKS_TESTLOG.txt ]]; then cp Schleifprozess_Assistent_2_95_DE_EN_WISSEN_LINKS_TESTLOG.txt "$PACKAGE/"; fi
          sha256sum "$PACKAGE/index.html" "$PACKAGE/manifest.webmanifest" "$PACKAGE/sw.js" "$PACKAGE/assets/"*.png > "$PACKAGE/SHA256SUMS.txt"
          grep -qi '<!doctype html' "$PACKAGE/index.html"
          grep -qi '</html>' "$PACKAGE/index.html"
          zip -r "$PACKAGE.zip" "$PACKAGE"
          test -s "$PACKAGE.zip"
          echo "PACKAGE_FILE=$PACKAGE.zip" >> "$GITHUB_ENV"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          if gh release view "$VERSION" >/dev/null 2>&1; then
            echo "Release $VERSION already exists; refusing to overwrite it."
            exit 1
          fi
          gh release create "$VERSION" "$PACKAGE_FILE" --title "Schleifprozess Assistent $VERSION" --notes "Offline-Version der validierten Deutsch-/Englisch-Fassung mit erweiterter Wissensdatenbank und Herstellerlinks. ZIP vollständig entpacken und index.html öffnen."
