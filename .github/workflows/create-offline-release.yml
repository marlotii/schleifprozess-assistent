name: Create Offline Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version, beispielsweise v2.95.1"
        required: true
        default: "v2.95.1"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Determine version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            VERSION="${GITHUB_REF_NAME}"
          else
            VERSION="${{ inputs.version }}"
          fi
          [[ "$VERSION" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Validate application source
        shell: bash
        run: |
          set -euo pipefail
          test -s index.html
          grep -q '<title>Schleifprozess Assistent</title>' index.html
          grep -q 'id="v295-functional-fix-js"' index.html
          grep -q 'function parseNumber295' index.html
          ! grep -q 'mobile.css' index.html
          ! grep -q 'mobile-setup.js' index.html
          test "$(stat -c%s index.html)" -eq 918985
          test "$(sha256sum index.html | cut -d' ' -f1)" = "2aa683ef8de047fc6f56ba4cfd49bb9904d3622ec7064520a4bfb70070af53a9"
          test "$(git hash-object index.html)" = "d9d4bdc47f65e5c89b1beac1656225d97bd09011"

      - name: Build offline package
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          PACKAGE="Schleifprozess_Assistent_Offline_${VERSION}"
          rm -rf "$PACKAGE" "$PACKAGE.zip"
          mkdir -p "$PACKAGE/assets"
          cp index.html manifest.webmanifest sw.js README-OFFLINE.txt "$PACKAGE/"
          cp assets/icon-192.png assets/icon-512.png assets/apple-touch-icon.png "$PACKAGE/assets/"
          if [[ -f Schleifprozess_Assistent_2_95_BEREINIGT_PRUEFBERICHT.md ]]; then cp Schleifprozess_Assistent_2_95_BEREINIGT_PRUEFBERICHT.md "$PACKAGE/"; fi
          if [[ -f Schleifprozess_Assistent_2_95_BEREINIGT_TESTLOG.txt ]]; then cp Schleifprozess_Assistent_2_95_BEREINIGT_TESTLOG.txt "$PACKAGE/"; fi
          sha256sum "$PACKAGE/index.html" "$PACKAGE/manifest.webmanifest" "$PACKAGE/sw.js" "$PACKAGE/assets/"*.png > "$PACKAGE/SHA256SUMS.txt"
          grep -qi '<!doctype html' "$PACKAGE/index.html"
          grep -qi '</html>' "$PACKAGE/index.html"
          zip -r "$PACKAGE.zip" "$PACKAGE"
          test -s "$PACKAGE.zip"
          echo "PACKAGE_FILE=$PACKAGE.zip" >> "$GITHUB_ENV"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          if gh release view "$VERSION" >/dev/null 2>&1; then
            echo "Release $VERSION already exists; refusing to overwrite it."
            exit 1
          fi
          gh release create "$VERSION" "$PACKAGE_FILE" --title "Schleifprozess Assistent $VERSION" --notes "Offline-Version der validierten bereinigten 2.95-Einzeldatei. ZIP vollständig entpacken und index.html öffnen."
